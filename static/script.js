/** SmartMetrology AI · SIH26034 · Government inspection workflow prototype */
document.addEventListener("DOMContentLoaded", () => {
  const $ = (s, root = document) => root.querySelector(s);
  const $$ = (s, root = document) => [...root.querySelectorAll(s)];
  const esc = (v = "") => String(v).replace(/[&<>'"]/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));
  const names = {dashboard:"Inspector Dashboard",scanner:"New Inspection",processing:"AI Processing",extracted:"Extracted Information",compliance:"Compliance Result",evidence:"Violation Evidence",review:"Human Review",report:"Inspection Report",rules:"Rule Database",analytics:"Analytics",history:"Inspection History",reports:"Reports"};
  const pipeline = ["Image enhancement","Text region detection","OCR extraction","AI/NLP field mapping","Rule matching","Compliance analysis"];
  const panelNames = ["Front image","Back image","Side image","Additional label image"];
  const defaultAudit = {
    inspection_id:"LM-20260907-AI26034", product_name:"Fresh Farm Atta", timestamp:new Date().toISOString(), overall_status:"FAIL", compliance_score:82, compliance_grade:"B", violations_count:2, warnings_count:1,
    panels:[], audit_report:{
      product:{title:"Product Name",rule:"Rule 6(1)(b)",status:"COMPLIANT",detected_value:"Fresh Farm Atta",details:"Common or generic product name is clearly declared."},
      manufacturer:{title:"Manufacturer / Packer",rule:"Rule 6(1)(a)",status:"COMPLIANT",detected_value:"ABC Foods Pvt Ltd",details:"Manufacturer identity and address are present."},
      quantity:{title:"Net Quantity",rule:"Rule 6(1)(c)",status:"COMPLIANT",detected_value:"1 kg",details:"Quantity uses an approved SI unit."},
      mrp:{title:"MRP inclusive of taxes",rule:"Rule 6(1)(e)",status:"COMPLIANT",detected_value:"₹120 (incl. of all taxes)",details:"Retail sale price includes the prescribed tax declaration."},
      date:{title:"Manufacturing / Packing Date",rule:"Rule 6(1)(d)",status:"COMPLIANT",detected_value:"Aug 2026",details:"Month and year of packing are visible."},
      care:{title:"Consumer Care Details",rule:"Rule 6(1)(da)",status:"NON_COMPLIANT",detected_value:"Not detected",details:"Mandatory consumer grievance contact information is missing.",bbox_reference:[74,390,620,105]},
      usp:{title:"Unit Sale Price",rule:"Rule 6(11)",status:"NON_COMPLIANT",detected_value:"Not detected",details:"Unit sale price declaration is missing or cannot be verified.",bbox_reference:[80,510,590,90]},
      quality:{title:"OCR quality",rule:"Evidence quality policy",status:"WARNING",detected_value:"82%",details:"One label region should be verified by an inspector."}
    }
  };
  let audit = structuredClone(defaultAudit), files = [], activeViolation = 0, review = null;
  const recent = [
    ["LM-260907-1042","Fresh Farm Atta","ABC Foods Pvt Ltd","07 Sep 2026","82%","FAIL","Pending"],
    ["LM-260906-0921","Royal Delight Cookies","Royal Foods India","06 Sep 2026","100%","PASS","A. Kumar"],
    ["LM-260905-1814","Himalayan Desi Ghee","North Valley Foods","05 Sep 2026","76%","FAIL","S. Meena"],
    ["LM-260904-1358","Herbal Hair Oil","Ayush Care Ltd","04 Sep 2026","91%","WARNING","Pending"]
  ];
  const seedRules = [
    {id:"LM-PC-001",number:"6(1)(a)",title:"Manufacturer / Packer Declaration",category:"General Packaged Commodity",from:"01 Jan 2026",to:"—",amendment:"G.S.R. 2026",status:"ACTIVE",version:2026},
    {id:"LM-PC-002",number:"6(1)(b)",title:"Common or Generic Product Name",category:"General Packaged Commodity",from:"01 Jan 2026",to:"—",amendment:"Base + 2026",status:"ACTIVE",version:2026},
    {id:"LM-PC-003",number:"6(1)(c)",title:"Net Quantity in Standard Units",category:"General Packaged Commodity",from:"01 Jan 2026",to:"—",amendment:"Rule 12 aligned",status:"ACTIVE",version:2026},
    {id:"LM-PC-004",number:"6(1)(e)",title:"MRP Inclusive of All Taxes",category:"Retail Package",from:"01 Jan 2026",to:"—",amendment:"G.S.R. 2026",status:"ACTIVE",version:2026},
    {id:"LM-PC-005",number:"6(1)(da)",title:"Consumer Grievance Details",category:"General Packaged Commodity",from:"01 Jan 2026",to:"—",amendment:"2022 / 2026",status:"ACTIVE",version:2026},
    {id:"LM-PC-006",number:"6(11)",title:"Unit Sale Price Declaration",category:"Retail Package",from:"01 Oct 2022",to:"—",amendment:"G.S.R. 2022",status:"ACTIVE",version:2022}
  ];
  let rules;
  try { rules = JSON.parse(localStorage.getItem("sm-rules")) || seedRules; } catch (_) { rules = seedRules; }

  function showScreen(name) {
    $$(".screen").forEach(x => x.classList.toggle("active", x.id === `screen-${name}`));
    $$(".nav-item").forEach(x => x.classList.toggle("active", x.dataset.screen === name));
    $("#page-title").textContent = names[name] || "SmartMetrology AI";
    $("#sidebar").classList.remove("open"); window.scrollTo({top:0,behavior:"smooth"});
    if (name === "extracted") renderFields(); if (name === "compliance") renderCompliance(); if (name === "evidence") renderEvidence();
    if (name === "review") renderReview(); if (name === "report") renderReport(); if (name === "rules") renderRules(); if (name === "analytics") loadAnalytics();
  }
  $$('[data-screen], [data-go]').forEach(b => b.addEventListener("click", () => showScreen(b.dataset.screen || b.dataset.go)));
  $("#mobile-menu").addEventListener("click", () => $("#sidebar").classList.toggle("open"));
  function toast(message, type = "") { const t=$("#toast"); t.textContent=message; t.className=`toast show ${type}`; clearTimeout(toast.timer); toast.timer=setTimeout(()=>t.className="toast",3000); }
  function openModal(title, html) { $("#modal-title").textContent=title; $("#modal-content").innerHTML=html; $("#modal").classList.add("show"); }
  function closeModal(){ $("#modal").classList.remove("show"); }
  $("#modal-close").addEventListener("click",closeModal); $("#modal").addEventListener("click",e=>{if(e.target.id==="modal")closeModal();});
  $("#notifications").addEventListener("click",()=>openModal("Review notifications",'<div class="review-alert warning"><b>3 high-priority reviews</b><span>Low-confidence extractions require inspector verification.</span></div><button class="btn btn-primary" id="open-pending">OPEN HUMAN REVIEW</button>')); document.addEventListener("click",e=>{if(e.target.id==="open-pending"){closeModal();showScreen("review");}});

  function renderHistory() {
    const rows=[...recent]; if(audit.inspection_id && !rows.some(r=>r[0]===audit.inspection_id)) rows.unshift([audit.inspection_id,audit.product_name,"Extracted label entity",new Date(audit.timestamp).toLocaleDateString("en-IN"),`${audit.compliance_score}%`,audit.overall_status,review?"A. Kumar":"Pending"]);
    const base=rows.map(r=>`<tr><td class="mono">${esc(r[0])}</td><td><b>${esc(r[1])}</b></td><td>${esc(r[2])}</td><td>${esc(r[3])}</td><td><b>${esc(r[4])}</b></td><td><span class="badge ${r[5]==="PASS"?"pass":r[5]==="WARNING"?"warning":"fail"}">${esc(r[5])}</span></td><td>${esc(r[6])}</td></tr>`).join("");
    $("#recent-body").innerHTML=base; $("#history-body").innerHTML=base.replaceAll("</tr>",'<td><button class="text-button" data-history-open>Open →</button></td></tr>'); $$('[data-history-open]').forEach(b=>b.addEventListener("click",()=>showScreen("report")));
  }

  $$(".upload-slot").forEach(s=>s.addEventListener("click",()=>{ $("#image-input").dataset.target=s.dataset.index; $("#image-input").click(); }));
  $("#image-input").addEventListener("change",e=>{ const incoming=[...e.target.files].filter(f=>f.type.startsWith("image/")).slice(0,4); const target=Number(e.target.dataset.target||0); incoming.forEach((f,i)=>files[Math.min(target+i,3)]=f); files=files.slice(0,4); e.target.value=""; renderUploads(); });
  $("#upload-grid").addEventListener("dragover",e=>e.preventDefault()); $("#upload-grid").addEventListener("drop",e=>{e.preventDefault();files=[...e.dataTransfer.files].filter(f=>f.type.startsWith("image/")).slice(0,4);renderUploads();});
  function renderUploads(){
    $$(".upload-slot").forEach((slot,i)=>{ [$("img",slot),$(".slot-overlay",slot),$(".quality-badge",slot),$(".remove-image",slot)].forEach(x=>x&&x.remove()); if(!files[i])return; const img=document.createElement("img"); img.src=URL.createObjectURL(files[i]); img.alt=panelNames[i]; const badge=document.createElement("span"); badge.className="quality-badge"; badge.textContent=files[i].size<80000?"⚠ CHECK QUALITY":"✓ GOOD QUALITY"; if(files[i].size<80000) badge.style.background="#b45309"; const ov=document.createElement("div"); ov.className="slot-overlay"; ov.innerHTML=`<b>${esc(panelNames[i])}</b><small>${esc(files[i].name)} · Click to retake</small>`; const remove=document.createElement("button"); remove.className="remove-image"; remove.type="button"; remove.textContent="×"; remove.title="Remove image"; remove.addEventListener("click",ev=>{ev.stopPropagation();files.splice(i,1);renderUploads();}); slot.append(img,badge,ov,remove); }); $("#start-inspection").disabled=files.length===0;
  }
  $("#start-inspection").addEventListener("click",()=>runInspection(false)); $("#demo-scan").addEventListener("click",()=>runInspection(true));
  async function runInspection(demo){
    showScreen("processing"); let stage=0; renderPipeline(stage); const timer=setInterval(()=>{stage=Math.min(stage+1,5);renderPipeline(stage);},650);
    try { let res; if(demo) res=await fetch("/api/sample/missing_grievance"); else { const fd=new FormData(); files.forEach(f=>fd.append("files",f)); res=await fetch("/api/audit",{method:"POST",body:fd}); } if(!res.ok){let d={};try{d=await res.json()}catch(_){ }throw new Error(d.detail||`Inspection service returned ${res.status}`);} audit=await res.json(); clearInterval(timer); renderPipeline(6); await new Promise(r=>setTimeout(r,550)); buildFields(); renderHistory(); showScreen("extracted"); toast("AI extraction complete. Verify fields before rule validation."); }
    catch(err){clearInterval(timer);$("#ocr-live").textContent=`ERROR STATE\n${err.message}\n\nNo compliance decision was created.`;$("#progress-label").textContent="Processing failed";toast("Inspection failed. Check images or service connection.","error");}
  }
  function renderPipeline(stage){ $("#pipeline-progress").style.width=`${Math.round(stage/6*100)}%`; $("#progress-label").textContent=stage>=6?"100% complete":`${Math.round(stage/6*100)}% complete`; $("#pipeline-steps").innerHTML=pipeline.map((p,i)=>`<div class="pipeline-step ${i<stage?"done":i===stage?"running":""}"><span>${i<stage?"✓":i===stage?"◉":"○"}</span><b>${p}</b></div>`).join(""); const snippets=["Preparing secure image workspace…","Enhancing contrast and correcting orientation…","Detected declaration text regions…","MRP ₹120\nNET QTY 1 kg\nPKD AUG 2026","Mapped declarations to statutory fields…","Matching effective rule version…","Extraction complete. Human verification is ready."]; $("#ocr-live").textContent=snippets[Math.min(stage,6)]; $("#ocr-confidence").textContent=stage<2?"—":`${Math.min(96,76+stage*4)}% confidence`; }

  function findRule(fragment){const hit=Object.values(audit.audit_report||{}).find(x=>x&&typeof x==="object"&&((x.title||"").toLowerCase().includes(fragment)||(x.rule||"").toLowerCase().includes(fragment)));return hit||{};}
  function confidence(item, fallback=90){const s=String(item.status||"").toUpperCase();return s==="WARNING"?78:s.includes("COMPLIANT")&&!s.includes("NON")?Math.max(fallback,94):Math.max(fallback,92);}
  function buildFields(){ const product=findRule("product name"),maker=findRule("manufacturer"),qty=findRule("net quantity"),mrp=findRule("mrp"),date=findRule("date"),care=findRule("consumer"),usp=findRule("unit sale"); audit.fields=[{name:"Product Name",value:audit.product_name||product.detected_value||"Not detected",confidence:confidence(product,99),source:"Front image"},{name:"Manufacturer / Packer / Importer",value:maker.detected_value||"Not detected",confidence:confidence(maker,91),source:"Back image"},{name:"Country of Origin",value:"India",confidence:84,source:"Back image"},{name:"Net Quantity",value:qty.detected_value||"Not detected",confidence:confidence(qty,97),source:"Front image"},{name:"MRP",value:mrp.detected_value||"Not detected",confidence:confidence(mrp,98),source:"Back image"},{name:"MRP inclusive of taxes",value:mrp.has_tax_clause===false?"No":"Yes",confidence:confidence(mrp,96),source:"Back image"},{name:"Manufacturing / Packing Date",value:date.detected_value||"Not detected",confidence:confidence(date,90),source:"Back image"},{name:"Best Before / Use By",value:"Not confidently detected",confidence:72,source:"Side image"},{name:"Consumer Care Details",value:care.detected_value||"Not detected",confidence:confidence(care,96),source:"Back image"},{name:"Unit Sale Price",value:usp.detected_value||"Not detected",confidence:confidence(usp,92),source:"Back image"},{name:"Dimensions (where applicable)",value:"Not applicable",confidence:95,source:"Inspector input"}]; }
  if(!audit.fields)buildFields();
  function renderFields(){ $("#field-grid").innerHTML=audit.fields.map((f,i)=>`<article class="field-card ${f.confidence<85?"warning":""}"><div><span>${esc(f.name)}</span><strong>${esc(f.value)}</strong><small>${f.confidence<85?"⚠ HUMAN REVIEW · ":""}${f.confidence}% confidence · ${esc(f.source)}</small></div><button data-edit-field="${i}">EDIT</button><div class="confidence-bar"><i style="width:${f.confidence}%"></i></div></article>`).join(""); $$('[data-edit-field]').forEach(b=>b.addEventListener("click",()=>editField(Number(b.dataset.editField)))); }
  function editField(i){const f=audit.fields[i];openModal(`Edit ${f.name}`,`<form class="modal-form" id="field-form"><label>Verified value<input id="field-value" value="${esc(f.value)}" required></label><label>Evidence source<input value="${esc(f.source)}" disabled></label><button class="btn btn-primary">SAVE VERIFIED VALUE</button></form>`);$("#field-form").addEventListener("submit",e=>{e.preventDefault();f.value=$("#field-value").value;f.confidence=100;f.source="Inspector verified";closeModal();renderFields();toast("Field verified and audit trail updated.");});}
  $("#edit-all").addEventListener("click",()=>{const i=audit.fields.findIndex(f=>f.confidence<85);editField(i<0?0:i);});

  function statusOf(x){const s=String(x||"").toUpperCase();return s==="WARNING"?"WARNING":s.includes("COMPLIANT")&&!s.includes("NON")||s==="PASS"?"PASS":"FAIL";}
  function entries(){return Object.entries(audit.audit_report||{}).filter(([,v])=>v&&typeof v==="object"&&(v.title||v.rule||v.status));}
  function renderCompliance(){ const es=entries(),counts={PASS:0,WARNING:0,FAIL:0};es.forEach(([,v])=>counts[statusOf(v.status)]++);const state=String(audit.overall_status||"").toUpperCase()==="PASS";$("#result-status").textContent=state?"COMPLIANT":"NON-COMPLIANT";$("#result-copy").textContent=state?"All evaluated declarations passed the selected rule version.":"Statutory declaration failures require accountable inspector review.";$(".result-hero").classList.toggle("pass-state",state);$(".result-hero").classList.toggle("fail",!state);$("#screening-score").textContent=`${Math.round(audit.compliance_score||0)}%`;$("#pass-count").textContent=counts.PASS;$("#warn-count").textContent=counts.WARNING;$("#fail-count").textContent=counts.FAIL;$("#compliance-body").innerHTML=es.map(([,v])=>{const s=statusOf(v.status);return `<tr><td><b>${esc(v.title||v.rule||"Requirement")}</b><small class="mono"> ${esc(v.rule||"")}</small></td><td><span class="badge ${s.toLowerCase()}">${s}</span></td><td><b>${confidence(v,94)}%</b></td><td>${esc(v.details||"Evaluated by the deterministic rule engine.")}</td></tr>`}).join(""); }
  function violations(){return entries().filter(([,v])=>statusOf(v.status)!=="PASS").map(([key,v])=>({key,...v,confidence:confidence(v,92)}));}
  function renderEvidence(){ const list=violations();if(!list.length){$("#violation-title").textContent="No violations detected";$("#violation-why").textContent="All evaluated declarations passed.";return;}activeViolation=Math.max(0,Math.min(activeViolation,list.length-1));const v=list[activeViolation],panel=audit.panels?.[v.panel_index||0];$("#violation-index").textContent=`Violation ${activeViolation+1} of ${list.length}`;$(".evidence-detail>.badge").textContent=`VIOLATION #${String(activeViolation+1).padStart(2,"0")}`;$("#violation-title").textContent=v.title||"Declaration requires review";$("#violation-why").textContent=v.details||"The required declaration could not be validated.";$("#violation-rule").textContent=`Legal Metrology (Packaged Commodities) Rules, 2011 · ${v.rule||"Applicable requirement"} · Version 2026`;$("#evidence-confidence").textContent=`${v.confidence}%`;$("#evidence-source").textContent=panel?.filename||"Back label image";$("#evidence-label").textContent=panel?.filename||"Back label image";const img=$("#evidence-img");img.src=panel?.image_b64||"/static/evidence-placeholder.svg";const box=$(".violation-box"),b=v.bbox_reference,dims=panel?.dimensions;if(Array.isArray(b)&&dims){const w=dims.width||dims[0]||800,h=dims.height||dims[1]||800;box.style.left=`${b[0]/w*100}%`;box.style.top=`${b[1]/h*100}%`;box.style.width=`${b[2]/w*100}%`;box.style.height=`${b[3]/h*100}%`;}else{box.style.left="18%";box.style.top="55%";box.style.width="65%";box.style.height="18%";} }
  $("#prev-violation").addEventListener("click",()=>{activeViolation--;renderEvidence();});$("#next-violation").addEventListener("click",()=>{activeViolation++;renderEvidence();});
  $$(".review-action").forEach(b=>b.addEventListener("click",()=>{if(b.textContent.includes("COMMENT")){openModal("Add evidence comment",'<form class="modal-form" id="comment-form"><label>Inspector comment<textarea id="evidence-comment" required></textarea></label><button class="btn btn-primary">SAVE COMMENT</button></form>');$("#comment-form").addEventListener("submit",e=>{e.preventDefault();audit.evidence_comment=$("#evidence-comment").value;closeModal();toast("Evidence comment saved.");});}else toast(`${b.textContent.trim()} recorded in the inspection audit trail.`);}));

  function renderReview(){const items=audit.fields.filter(f=>f.confidence<85).concat(violations().map(v=>({name:v.title,value:v.detected_value||"Not detected",confidence:v.confidence,source:v.rule})));$("#review-count").textContent=`${items.length} PENDING`;$("#review-items").innerHTML=items.length?items.map((x,i)=>`<div class="review-item"><div class="review-item-head"><div><b>${esc(x.name)}</b><small> · ${esc(x.source)}</small></div><span class="badge ${x.confidence<85?"warning":"fail"}">${x.confidence}%</span></div><input value="${esc(x.value)}" data-review-value="${i}" aria-label="Verified ${esc(x.name)}"><p>Inspector verification required before final report generation.</p></div>`).join(""):'<div class="empty-state"><span>✓</span><h2>No ambiguous items</h2><p>All extracted fields exceed the review threshold.</p></div>';}
  $("#complete-review").addEventListener("click",async()=>{review={inspector:"Arun Kumar",decision:$("#review-decision").value,comments:$("#review-comment").value||"No additional comments.",timestamp:new Date().toISOString()};$("#review-time").textContent=new Date(review.timestamp).toLocaleString("en-IN");try{await fetch("/api/reviews",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({inspection_id:audit.inspection_id,...review})});}catch(_){}toast("Human review completed and timestamped.");showScreen("report");});

  function renderReport(){const status=String(audit.overall_status).toUpperCase()==="PASS"?"COMPLIANT":"NON-COMPLIANT";$("#report-status").textContent=status;$("#report-status").className=`badge ${status==="COMPLIANT"?"pass":"fail"}`;const meta=[["Inspection ID",audit.inspection_id],["Date & time",new Date(audit.timestamp).toLocaleString("en-IN")],["Rule version","LM-PC Rules · 2026"],["Screening score",`${audit.compliance_score}%`],["Product images",`${audit.panels?.length||files.length||1} panel(s)`],["Decision source","Deterministic Rule Engine"]];$("#report-meta").innerHTML=meta.map(x=>`<div><span>${x[0]}</span><b>${esc(x[1])}</b></div>`).join("");$("#report-fields").innerHTML=audit.fields.map(f=>`<div><span>${esc(f.name)}</span><b>${esc(f.value)} · ${f.confidence}%</b></div>`).join("");const vs=violations();$("#report-violations").innerHTML=vs.length?vs.map(v=>`<div class="report-violation"><b>${esc(v.title)}</b><p>${esc(v.details)} · ${esc(v.rule)} · ${v.confidence}% confidence</p></div>`).join(""):'<div class="review-record">No failed statutory requirements.</div>';$("#review-record").textContent=review?`${review.decision} · ${review.inspector} · ${new Date(review.timestamp).toLocaleString("en-IN")} · ${review.comments}`:"Pending human review";}
  $("#generate-report").addEventListener("click",async()=>{const b=$("#generate-report"),old=b.textContent;b.disabled=true;b.textContent="GENERATING PDF…";try{const res=await fetch("/api/export-pdf",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({inspection_id:audit.inspection_id,commodity_name:audit.product_name,overall_status:audit.overall_status,compliance_score:Number(audit.compliance_score),grade:audit.compliance_grade||"—",report:audit.audit_report})});if(!res.ok)throw new Error(`PDF service returned ${res.status}`);const blob=await res.blob(),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download=`SmartMetrology_${audit.inspection_id}.pdf`;a.click();URL.revokeObjectURL(url);toast("Official-format PDF report downloaded.");}catch(e){toast(e.message,"error");}finally{b.disabled=false;b.textContent=old;}});

  function saveRules(){localStorage.setItem("sm-rules",JSON.stringify(rules));}
  function renderRules(){const q=$("#rule-search").value.toLowerCase(),filter=$("#rule-filter").value;const shown=rules.filter(r=>(filter==="All statuses"||r.status===filter)&&JSON.stringify(r).toLowerCase().includes(q));$("#rules-body").innerHTML=shown.map(r=>{const i=rules.indexOf(r);return `<tr><td class="mono">${esc(r.id)}<small> v${esc(r.version)}</small></td><td>${esc(r.number)}</td><td><b>${esc(r.title)}</b></td><td>${esc(r.category)}</td><td>${esc(r.from)} → ${esc(r.to)}</td><td>${esc(r.amendment)}</td><td><span class="badge ${r.status==="ACTIVE"?"pass":"warning"}">${r.status}</span></td><td><button class="text-button" data-rule-edit="${i}">Edit</button> · <button class="text-button" data-rule-version="${i}">New version</button> · <button class="text-button" data-rule-toggle="${i}">${r.status==="ACTIVE"?"Deactivate":"Activate"}</button></td></tr>`}).join("");$$('[data-rule-edit]').forEach(b=>b.onclick=()=>ruleForm(Number(b.dataset.ruleEdit)));$$('[data-rule-version]').forEach(b=>b.onclick=()=>{const r=rules[Number(b.dataset.ruleVersion)];rules.push({...r,id:`${r.id}-V${Number(r.version)+1}`,version:Number(r.version)+1,from:new Date().toLocaleDateString("en-IN"),amendment:"New administrator version",status:"INACTIVE"});saveRules();renderRules();toast("Immutable rule version created as inactive.");});$$('[data-rule-toggle]').forEach(b=>b.onclick=()=>{const r=rules[Number(b.dataset.ruleToggle)];r.status=r.status==="ACTIVE"?"INACTIVE":"ACTIVE";saveRules();renderRules();toast(`Rule ${r.status.toLowerCase()}.`);});}
  function ruleForm(index=null){const r=index===null?{id:`LM-PC-${String(rules.length+1).padStart(3,"0")}`,number:"",title:"",category:"General Packaged Commodity",from:new Date().toLocaleDateString("en-IN"),to:"—",amendment:"",status:"INACTIVE",version:2026}:rules[index];openModal(index===null?"Add statutory rule":"Edit rule metadata",`<form class="modal-form" id="rule-form"><label>Rule ID<input id="rf-id" value="${esc(r.id)}" required></label><label>Rule number<input id="rf-number" value="${esc(r.number)}" required></label><label>Rule title<input id="rf-title" value="${esc(r.title)}" required></label><label>Product category<input id="rf-category" value="${esc(r.category)}" required></label><label>Amendment / notification<input id="rf-amendment" value="${esc(r.amendment)}"></label><button class="btn btn-primary">SAVE RULE</button></form>`);$("#rule-form").onsubmit=e=>{e.preventDefault();const next={...r,id:$("#rf-id").value,number:$("#rf-number").value,title:$("#rf-title").value,category:$("#rf-category").value,amendment:$("#rf-amendment").value};if(index===null)rules.push(next);else rules[index]=next;saveRules();closeModal();renderRules();toast("Rule database updated.");};}
  $("#add-rule").onclick=()=>ruleForm();$("#rule-search").oninput=renderRules;$("#rule-filter").onchange=renderRules;$("#import-rule").onclick=()=>{openModal("Import amendment",'<form class="modal-form" id="import-form"><label>Government notification / amendment<input type="file" id="amendment-file" accept=".pdf,.json" required></label><p>The imported text remains inactive until an authorised administrator validates and activates its rule version.</p><button class="btn btn-primary">IMPORT AS DRAFT</button></form>');$("#import-form").onsubmit=e=>{e.preventDefault();closeModal();toast("Amendment imported as an inactive draft.");};};
  async function loadAnalytics(){try{const r=await fetch("/api/analytics");if(r.ok){const d=await r.json();$("#analytics-rate").textContent=`${d.pass_rate}%`;$("#metric-total").textContent=d.total_audited;$("#metric-pass").textContent=d.pass_count;$("#metric-fail").textContent=d.violations_count;}}catch(_){}const bars=[["Consumer care details",31],["Unit sale price",24],["MRP tax declaration",19],["Non-standard units",15],["Manufacturing date",11]];$("#violation-bars").innerHTML=bars.map(x=>`<div class="bar-row"><div><b>${x[0]}</b><span>${x[1]}%</span></div><div class="bar-track"><div class="bar-fill" style="width:${x[1]*2.6}%"></div></div></div>`).join("");const makers=[["ABC Foods Pvt Ltd","5 inspections · 3 violations"],["North Valley Foods","4 inspections · 2 violations"],["Ayush Care Ltd","7 inspections · 2 warnings"],["Royal Foods India","9 inspections · 96% compliant"]];$("#manufacturer-list").innerHTML=makers.map((x,i)=>`<div class="risk-row"><b>${x[0]}</b><span class="badge ${i<2?"fail":i===2?"warning":"pass"}">${i<2?"HIGH":i===2?"WATCH":"LOW"}</span><small>${x[1]}</small></div>`).join("");}
  $("#refresh-analytics").onclick=()=>{loadAnalytics();toast("Analytics refreshed from inspection ledger.");};$("#history-search").oninput=e=>{$$("#history-body tr").forEach(tr=>tr.hidden=!tr.textContent.toLowerCase().includes(e.target.value.toLowerCase()));};
  renderHistory();renderRules();loadAnalytics();
});
